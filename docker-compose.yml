services:
  postgres:
    image: postgres:16-alpine
    container_name: smartbot-postgres-only
    environment:
      POSTGRES_DB: smartbot
      POSTGRES_USER: smartbot
      POSTGRES_PASSWORD: smartbot
    ports:
      - "15432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: smartbot-redis-only
    ports:
      - "6379:6379"

volumes:
  postgres_data:

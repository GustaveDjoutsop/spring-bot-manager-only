spring:
  datasource:
    url: jdbc:postgresql://localhost:15432/smartbot
    driver-class-name: org.postgresql.Driver
    username: smartbot
    password: smartbot
  flyway:
    enabled: true
  jpa:
    hibernate:
      ddl-auto: validate
  data:
    redis:
      url: redis://localhost:6379

bot:
  config-directory: ../configs/bots

admin:
  username: testadmin
  password: testpass

jwt:
  secret: test-secret-key-for-integration-tests-only-do-not-use
  expiration-ms: 3600000

encryption:
  master-key: ""